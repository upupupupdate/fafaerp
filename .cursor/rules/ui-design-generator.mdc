---
description: 根据需求文档生成 UI 设计。触发词：/根据需求文档生成UI设计。将需求文档(.md)转换为高保真、扁平化、蓝色的 HTML 原型。
globs: output/**/*.html
alwaysApply: false
---

# 需求文档转UI设计生成规则

## 1. 触发指令 (标准提示词)
当用户输入以下内容或类似指令时，必须严格执行本规则：
> **/根据需求文档生成UI设计**
> 根据 @ui-design-generator.mdc 规则，将 @需求文档.md 生成UI设计稿。
> **设计风格**：扁平化设计
> **主色调**：蓝色
> **响应式**：是

## 2. 执行步骤
1. **读取文档**：分析用户指定的 `@需求文档.md` 文件内容。
2. **分析逻辑**：提取功能需求、字段定义及用户操作流程。
3. **UI 设计**：基于分析结果，生成符合现代趋势的扁平化界面。
4. **输出文件**：
   - 将设计转换为多个 HTML 文件输出到项目根目录的 `output/` 文件夹。
   - 每个独立系统以 `index.html` 作为首页。
   - 文件命名格式：`[项目名称]-ui-design.html`。

## 3. UI 设计生成指南

### 基本原则
- **界面风格**：简洁清晰，采用**扁平化设计**。
- **交互逻辑**：优先考虑用户体验，遵循文档中的用户流程。
- **布局要求**：必须是**响应式**布局，适配不同尺寸设备。
- **特色功能**：
  - 所有打开页面建议使用**多标签页**显示。
  - 列表头字段支持自定义设置显示。
  - 列表明细默认显示三条，提供一键展开/收起功能。
  - 列表分页：支持设置每页条数（20, 50, 100, 200）并支持快速跳转。

### 设计元素
- **色彩方案**：主色调统一为**蓝色**。
- **组件样式**：按钮背景必须为蓝色，表单样式需整齐统一。
- **导航结构**：包含清晰的导航栏和布局框架。

### 输出 HTML 要求
- **单文件结构**：每个页面为一个独立的 HTML，包含所有必要的 CSS 和 JS。
- **无外部依赖**：不依赖外部网络资源（除非指定 CDN），确保本地可直接打开。
- **注释结构**：代码结构合理，关键逻辑需有清晰注释。

## 4. 模板结构标准
生成的 HTML 必须遵循以下基础架构：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>[项目名称] UI设计</title>
    <style>
        /* 此处包含扁平化、蓝色调的 CSS 样式 */
    </style>
</head>
<body>
    <header>
        <!-- 导航及标签页 -->
    </header>
    <main>
        <!-- 核心功能区域 -->
    </main>
    <footer>
        <!-- 页脚信息 -->
    </footer>
    <script>
        /* 此处包含列表分页、展开收起等交互 JS 代码 */
    </script>
</body>
</html>